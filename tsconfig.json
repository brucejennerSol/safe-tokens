{
    "compilerOptions": {
      /* Basic Options */
      "target": "esnext",                        // Use ESNext for Bun, which supports modern JavaScript features.
      "module": "esnext",                        // Bun prefers ES module syntax, so set this to 'esnext'.
      "lib": ["esnext", "dom"],                  // Use 'esnext' to leverage Bun's modern runtime environment.
      "allowJs": true,                           // Allow JavaScript files to be compiled.
      "outDir": "./dist",                        // Redirect output structure to the 'dist' directory.
      "rootDir": "./src",                        // Specify the root directory of input files.
      "strict": true,                            // Enable all strict type-checking options.
      "esModuleInterop": true,                   // Ensures compatibility with CommonJS modules, if needed.
      "forceConsistentCasingInFileNames": true,  // Disallow inconsistently-cased references to the same file.
  
      /* Module Resolution Options */
      "moduleResolution": "node",                // Use Node.js-style module resolution (Bun is compatible with Node's ecosystem).
      "resolveJsonModule": true,                 // Allow importing JSON files.
      "isolatedModules": true,                   // Bun uses isolated modules, so enable this to avoid type conflicts.
      "noEmit": true,                            // Bun compiles TypeScript in-memory, so you may not need output JS files.
      
      /* Source Map Options */
      "sourceMap": true,                         // Enable source maps for easier debugging.
  
      /* Experimental Options */
      "experimentalDecorators": true,            // Enable decorators, if needed.
  
      /* Additional Bun Considerations */
      "useDefineForClassFields": true            // Required for Bun's compatibility with class fields (modern JS).
    },
    "include": ["src/**/*"],                     // Include all TypeScript files in the 'src' folder.
    "exclude": ["node_modules", "dist"]          // Exclude 'node_modules' and the output 'dist' folder.
  }
  